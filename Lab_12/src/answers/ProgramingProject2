public String toString(){
	StringBuilder sb = new StringBuilder();
	sb.append("[");
	for(E i = set){
		sb.append(i);
		sb.append(", ");
	}
	sb.replace(sb.length - 3, sb.length - 1, "]");
	return sb.toString();
}